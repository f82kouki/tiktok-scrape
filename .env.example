# 任意のプロキシ（住宅IPプロキシ推奨。データセンターIPはTikTokに弾かれやすい）
# 例: http://user:pass@proxy.example.com:8080
HTTP_PROXY=

# 1リクエストごとの待機秒数（min, max）
REQUEST_INTERVAL_MIN=4.0
REQUEST_INTERVAL_MAX=8.0

# 実行時にデバッグログを表示するか
DEBUG=true

# 非ヘッドレスで起動（デバッグ時のみ true）
HEADLESS=true
